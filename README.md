# Opsyn Backend - METRC Ops Hub ## Local Development pip install -r requirements.txt cp .env.example .env # edit .env with your keys uvicorn main:app --reload --port 8000 # API docs: http://localhost:8000/docs ## Railway Deployment 1. Push to GitHub 2. railway.app > New Project > Deploy from GitHub 3. Add env vars from .env.example 4. Auto-deploys from Dockerfile 5. URL: https://your-app.up.railway.app ## Render Deployment 1. Push to GitHub 2. render.com > New Web Service > Connect repo 3. Build: pip install -r requirements.txt 4. Start: uvicorn main:app --host 0.0.0.0 --port $PORT 5. Add env vars 6. URL: https://your-app.onrender.com ## Test Request curl -X POST https://your-url/api/ai/chat \ -H "Content-Type: application/json" \ -H "X-API-Key: opsyn_live_your_key_here" \ -H "X-User-ID: preston" \ -H "X-User-Role: admin" \ -H "X-Facility-License: PAAA-VJZG-KNP6" \ -d '{"message": "What packages need testing?", "context": {"screen": "testing"}}'